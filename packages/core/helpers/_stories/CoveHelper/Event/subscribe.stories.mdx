import { Canvas, Meta, Story, ArgsTable } from '@storybook/blocks';

<Meta
  title="Helpers/CoveHelper/Event/subscribe"
/>

# subscribe
This function registers an event listener for a given event name on the document object. When the specified event is triggered,
the listener function will be executed.

The function returns the result of the `addEventListener` method call.

See the [unsubscribe](/?path=/docs/helpers-covehelper-event-unsubscribe--docs) function for removing the event listener.

&nbsp;
## Syntax
```js
CoveHelper.Event.subscribe(eventName, listener);
```

**Parameters**

- `eventName`: A string representing the name of the event to listen for.
- `listener`: A function to be executed when the specified event is triggered.

&nbsp;
### Example
In this example, the `handleClick` function is registered as an event listener for the click event on a button element using the `subscribe` function.
```js
function handleClick(event) {
  console.log('Button clicked!', event.target);
}

const button = document.querySelector('button');
subscribe('click', handleClick);
```
