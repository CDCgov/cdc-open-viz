import { Canvas, Meta, Story, ArgsTable } from '@storybook/blocks';

import SplashError from '../SplashError'

<Meta
  title="Components/UI/SplashError"
  component={SplashError}
/>

# SplashError Component
A splash display to present errors to the user.

<Canvas sourceState="shown" withSource="open">
  <Story name="Example"
  args={{
    title: 'Error',
    message: 'Something went wrong with your component'
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example"/>

export const Template = ({ ...args }) => (
  <div style={{ height: '400px' }}>
    <SplashError {...args}/>
  </div>
)
