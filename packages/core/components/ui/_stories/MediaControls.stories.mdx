import { Canvas, Meta, Story, ArgsTable } from '@storybook/blocks';

import MediaControls from '../MediaControls'

<Meta
  title="Components/UI/MediaControls"
  component={MediaControls}
/>

# MediaControls Component
Used to generate download buttons for saving COVE related visualizations and/or data in various formats.

Pending further documentation.

<Canvas sourceState="shown" withSource="open">
  <Story name="Example" args={{
    children: [
      <MediaControls.Button text="Download Image" title="Download Chart as Image" type="image" elementToCapture={"imageId"}/>,
      <MediaControls.Button text="Download PDF" title="Download Chart as PDF" type="pdf" elementToCapture={"imageId"}/>,
      <MediaControls.Link config={"config"}/>
      ]
  }}>
    {Section.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example"/>

export const Section = ({ ...args }) => <MediaControls.Section {...args}/>
