import { Canvas, Meta, Story, ArgsTable } from '@storybook/blocks';

import LoadSpin from '../LoadSpin'

<Meta
  title="Components/UI/LoadSpin"
  component={LoadSpin}
/>

# LoadSpin Component
The LoadSpin component can be used to display pending states while fetching data, or rendering large datasets.

New components can also be built consume this animation sequence and provide a more custom display:

[ See **Components/Loader/RenderFallback** for working example ]

<Canvas sourceState="shown" withSource="open">
  <Story name="Example" args={{
    color: "#3a569b",
    opacity: 40,
    size: 70
  }}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example"/>

export const Template = ({...args}) => <LoadSpin {...args} />
