import { Canvas, Meta, Story, ArgsTable } from '@storybook/blocks';

import Tooltip from '../Tooltip'
import Icon from '../Icon'

<Meta
  title="Components/UI/Tooltip"
  component={Tooltip}
/>

# Tooltip
Use the Button component to provide users with a method to interface with a major component, or trigger certain actions.

## Trigger Type
<Canvas sourceState="shown" withSource="open">
  <Story name="Trigger Type">
    <Tooltip position="right" border={false}>
      <Tooltip.Target>
        <Icon display="question" alt="hello"/>
      </Tooltip.Target>
      <Tooltip.Content>
        Hello world! I have a <a href="#">link</a>.
      </Tooltip.Content>
    </Tooltip>
    <Tooltip position="left" trigger="click" border={false}>
      <Tooltip.Target>
        <Icon display="question" alt="hello"/>
      </Tooltip.Target>
      <Tooltip.Content>
        Hello world! I have a <a href="#">link</a>.
      </Tooltip.Content>
    </Tooltip>
  </Story>
</Canvas>

## Positioning
<Canvas sourceState="shown" withSource="open">
  <Story name="Position">
    <Tooltip position="top" trigger="click" border={false}>
      <Tooltip.Target>
        <Icon display="question" alt="hello"/>
      </Tooltip.Target>
      <Tooltip.Content>
        Hello world! I have a <a href="#">link</a>.
      </Tooltip.Content>
    </Tooltip>
    <Tooltip position="right" trigger="click" border={'1'} borderColor={'red'}>
      <Tooltip.Target>
        <Icon display="question" alt="hello"/>
      </Tooltip.Target>
      <Tooltip.Content>
        Hello world! I have a <a href="#">link</a>.
      </Tooltip.Content>
    </Tooltip>
    <Tooltip position="bottom" trigger="click" shadow={false} border={'2'}>
      <Tooltip.Target>
        <Icon display="question" alt="hello"/>
      </Tooltip.Target>
      <Tooltip.Content>
        Hello world! I have a <a href="#">link</a>.
      </Tooltip.Content>
    </Tooltip>
    <Tooltip position="left" trigger="click" border={'2'} borderColor={'blue'}>
      <Tooltip.Target>
        <Icon display="question" alt="hello"/>
      </Tooltip.Target>
      <Tooltip.Content>
        Hello world! I have a <a href="#">link</a>.
      </Tooltip.Content>
    </Tooltip>
  </Story>
</Canvas>
