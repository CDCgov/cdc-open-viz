import { Canvas, Meta, Story, ArgsTable } from '@storybook/blocks';

import RenderFallback from '../RenderFallback'

<Meta
  title="Components/Loader/RenderFallback"
  component={RenderFallback}
/>

# RenderFallback Component
Used to provide an animated display while loading or processing any asynchronous data for components or visualizations.

<Canvas sourceState="shown" withSource="open">
  <Story name="Example"
         args={{
           fadeEffect: true,
           text: 'Rendering visualization...',
           loadSpinColor: '#005eaa',
           loadSpinOpacity: 30,
           loadSpinSize: 85
         }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example"/>

export const Template = ({ ...args }) => <RenderFallback {...args}/>
