import { Meta, Story, ArgsTable, Canvas } from '@storybook/blocks';
import InputCheckbox from '../InputCheckbox'

<Meta
  title="Components/Input/Checkbox"
  component={InputCheckbox}
  parameters={{ options: { isToolshown: true } }}
/>

export const Template = ({ ...args }) => (
  <InputCheckbox {...args} />
)

# Checkbox Component
Component that renders a checkbox input, with an optional label and tooltip. It receives a set of props that can be used to customize its behavior and appearance.

<Canvas sourceState="shown" withSource="open">
  <Story name="Example"
         args={{
           label: 'Use An Option',
           labelPosition: 'right',
           size: 'medium',
           activeColor: '#025eaa',
           activeCheckColor: '#fff',
           tooltip: 'This is a tooltip',
         }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example"/>
