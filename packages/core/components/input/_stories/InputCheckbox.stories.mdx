import { Meta, Story, ArgsTable, Canvas } from '@storybook/blocks';
import InputCheckbox from '../InputCheckbox'

<Meta
  title="Components/Input/InputCheckbox"
  component={InputCheckbox}
  parameters={{ options: { isToolshown: true } }}
/>

# InputCheckbox Component
Renders a checkbox input, with an optional label and tooltip.

<Canvas sourceState="shown" withSource="open">
  <Story name="Example"
         args={{
           label: 'Use An Option',
           labelPosition: 'right',
           size: 'medium',
           activeColor: '#025eaa',
           activeCheckColor: '#fff',
           tooltip: 'This is an example tooltip.',
           stretch: false
         }}
         argTypes={{
           configField: {
             control: {
               disable: true
             },
           }
         }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Example"/>

export const Template = ({ ...args }) => (
  <InputCheckbox {...args} />
)
